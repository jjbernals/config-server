server:
  port: 8081

debug:
  enable: true

spring:
  application:
    name: account

  datasource:
    url: jdbc:mysql://localhost:3306/account
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
        dialect: com.mysql

